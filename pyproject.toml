[project]
name = "test-learn-zeit-structure"
version = "0.1.0"
description = "Minimal PostgREST + Alembic + Node + k8s learning sandbox"
requires-python = ">=3.12"
dependencies = [
    "alembic",
    "psycopg2-binary",
]

[tool.uv]
package = false
